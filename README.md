# boost_serialization_example

The repo is used to document the example usage of serialization.

Some issue happens on real case. For example, the memorization is to resovle the call stack overflow issue.
